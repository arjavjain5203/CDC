#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
    /*
    🔹 Problem Statement:
    Given a string, count the number of words. Words are separated by spaces.

    🔹 Approach:
    - Initialize a counter 'spaces' to 0.
    - Traverse the string character by character.
    - Increment 'spaces' whenever a space (' ') is encountered.
    - The number of words = spaces + 1.

    ⚙️ Time Complexity: O(n), where n = length of the string
    ⚙️ Space Complexity: O(1), constant extra space
    */

    int countWords(const string &str) {
        int n = str.length();
        int spaces = 0;

        for(int i = 0; i < n; i++) {
            if(str[i] == ' ') {
                spaces++;
            }
        }

        return spaces + 1; // Number of words = number of spaces + 1
    }
};
